# =========================================
# libs.versions.toml
# Organized, commented, and ready to go.
# This Code is Formated By CHAT-GPT
# Please Maintain this Format So everyone can code eaisily :)
# =========================================

# ──────────────────────────────────────────────────────────────────────────────
# Versions
# Keep all version pins here. Libraries below reference these with `version.ref`.
# ──────────────────────────────────────────────────────────────────────────────
[versions]

# ▶ Toolchain / Plugins
agp = "8.13.0"                       # Android Gradle Plugin
kotlin = "2.2.10"                    # Kotlin language version
ksp = "2.2.0-2.0.2"                  # Kotlin Symbol Processing plugin

# ▶ AndroidX Core / AppCompat
coreKtx = "1.17.0"                   # androidx.core:core-ktx
appcompat = "1.7.1"                  # androidx.appcompat:appcompat

# ▶ Compose / UI
composeBom = "2025.08.01"            # Compose BOM (controls Compose artifacts without explicit versions)
material3 = "1.5.0-alpha03"           # androidx.compose.material3:material3
material = "1.13.0"                   # androidx.compose.material:material
materialIconsExtended = "1.7.8"      # androidx.compose.material:material-icons-extended
activityCompose = "1.10.1"           # androidx.activity:activity-compose
animation = "1.9.0"                  # androidx.compose.animation:animation
runtimeAndroid = "1.9.0"             # androidx.compose.runtime:runtime-android

# ▶ Lifecycle
lifecycleRuntimeKtx = "2.9.3"
lifecycleViewmodelKtx = "2.9.3"
lifecycleRuntimeCompose = "2.9.3"
lifecycleViewmodelCompose = "2.9.3"

# ▶ Navigation
navigationCompose = "2.9.3"
accompanistInsets = "0.30.1"         # Insets (legacy)
accompanistInsetsUi = "0.36.0"       # Insets/UI + nav-animation version

# ▶ Data & Persistence
roomRuntime = "2.7.2"                # Room (runtime/ktx/compiler share this)
datastorePreferences = "1.1.7"       # DataStore Preferences

# ▶ Background Work
workRuntimeKtx = "2.10.3"            # WorkManager

# ▶ Auth / Biometrics
biometric = "1.2.0-alpha05"

# ▶ JSON / Serialization
kotlinxSerializationJson = "1.9.0"   # kotlinx-serialization-json
moshi = "1.15.2"
moshiKotlin = "1.15.2"
gson = "2.13.1"

# ▶ Networking / Parsing
okhttp = "5.1.0"
jsoup = "1.21.2"

# ▶ Testing
junit = "4.13.2"
junitVersion = "1.3.0"               # androidx.test.ext:junit
espressoCore = "3.7.0"

# ▶ Java 8+ APIs on old devices
desugar_jdk_libs = "2.1.5"


# ──────────────────────────────────────────────────────────────────────────────
# Libraries
# No renames, no removals—just grouped with comments.
# ──────────────────────────────────────────────────────────────────────────────
[libraries]

# ▶ Networking & Parsing
jsoup = { module = "org.jsoup:jsoup", version.ref = "jsoup" }                               # HTML parser
okhttp = { module = "com.squareup.okhttp3:okhttp", version.ref = "okhttp" }                 # HTTP client

# ▶ JSON / Serialization
moshi = { module = "com.squareup.moshi:moshi", version.ref = "moshi" }
moshi-kotlin = { module = "com.squareup.moshi:moshi-kotlin", version.ref = "moshiKotlin" }
gson = { group = "com.google.code.gson", name = "gson", version.ref = "gson" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerializationJson" }

# ▶ Accompanist / Navigation / Animation
accompanist-insets = { module = "com.google.accompanist:accompanist-insets", version.ref = "accompanistInsets" }
accompanist-insets-ui = { module = "com.google.accompanist:accompanist-insets-ui", version.ref = "accompanistInsetsUi" }
accompanist-navigation-animation = { module = "com.google.accompanist:accompanist-navigation-animation", version.ref = "accompanistInsetsUi" }
androidx-animation = { module = "androidx.compose.animation:animation", version.ref = "animation" }
androidx-navigation-compose = { module = "androidx.navigation:navigation-compose", version.ref = "navigationCompose" }

# ▶ AndroidX Core / AppCompat / Biometrics
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }
androidx-biometric = { module = "androidx.biometric:biometric", version.ref = "biometric" }

# ▶ Lifecycle
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycleRuntimeKtx" }
androidx-lifecycle-viewmodel-ktx = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-ktx", version.ref = "lifecycleViewmodelKtx" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycleRuntimeCompose" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycleViewmodelCompose" }

# ▶ Data & Persistence
androidx-datastore-preferences = { module = "androidx.datastore:datastore-preferences", version.ref = "datastorePreferences" }
androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "roomRuntime" }
androidx-room-ktx = { module = "androidx.room:room-ktx", version.ref = "roomRuntime" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "roomRuntime" } # Use KSP/apt depending on setup

# ▶ WorkManager
androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "workRuntimeKtx" }

# ▶ Compose / UI
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" } # Import in dependencies to manage Compose versions
androidx-material3 = { group = "androidx.compose.material3", name = "material3", version.ref = "material3" }
androidx-material = { group = "com.google.android.material", name = "material", version.ref = "material" }

androidx-material-icons-extended = { module = "androidx.compose.material:material-icons-extended", version.ref = "materialIconsExtended" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-ui = { group = "androidx.compose.ui", name = "ui" }                                      # Version via BOM
androidx-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }                   # Version via BOM
androidx-runtime-android = { group = "androidx.compose.runtime", name = "runtime-android", version.ref = "runtimeAndroid" }
desugar_jdk_libs = { module = "com.android.tools:desugar_jdk_libs", version.ref = "desugar_jdk_libs" }

# ▶ Compose Tooling (debug/dev only typically)
androidx-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }                      # Version via BOM
androidx-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }      # Version via BOM

# ▶ Testing
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
androidx-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }          # Version via BOM
androidx-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }              # Version via BOM


# ──────────────────────────────────────────────────────────────────────────────
# Plugins
# Keep plugin coords here; versions either use refs above or explicit strings.
# ──────────────────────────────────────────────────────────────────────────────
[plugins]

# ▶ Android Gradle
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }

# ▶ Kotlin
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version = "2.2.10" }   # matches Kotlin

# ▶ KSP
kotlin-ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
